# DJ Kimchi — Claude Project Instructions

## Project Identity

**What it is:** Official website for DJ Kimchi — Nairobi-based DJ specialising in Afrobeats, Amapiano, Gengetone, and Dancehall.
**Framework:** Next.js 16 App Router — React 19, TypeScript, Tailwind v4, Shadcn/ui
**Package manager:** Bun (use `bun install`, `bun run dev`, `bun run build`)
**Deployed:** Vercel. Set `NEXT_PUBLIC_SITE_URL` in Vercel env vars to the production domain.
**Repo:** https://github.com/rauell1/dj-kimchi.git

---

## Architecture

```
src/app/
  layout.tsx        — root layout: metadata, JSON-LD, Google/Bing verification,
                       Navigation, Footer, GlobalPlayer, ScrollToTop
  page.tsx          — server component (SSR, NOT "use client") — renders HeroSection
  sitemap.ts        — /sitemap.xml (5 routes)
  robots.ts         — /robots.txt
  about/page.tsx    — /about (SSR, exports metadata with canonical)
  music/page.tsx    — /music (SSR, exports metadata with canonical)
  videos/page.tsx   — /videos (SSR, exports metadata with canonical)
  bookings/page.tsx — /bookings (SSR, exports metadata with canonical)
  api/
    booking/        — booking form email handler (Resend)
    mixcloud-url/   — proxy to Mixcloud API
    webhooks/inbound/

src/components/site/
  navigation.tsx    — sticky nav, uses next/link for routes, theme toggle
  hero.tsx          — hero section with /music and /bookings CTAs
  about.tsx         — about section
  music.tsx         — mixes / HearThis player
  videos.tsx        — YouTube embeds
  photos.tsx        — photo gallery
  bookings.tsx      — booking form (react-hook-form + Resend)
  footer.tsx
  global-player.tsx — persistent audio player (lives in layout, survives navigation)
  scroll-to-top.tsx — floating scroll-to-top button

src/lib/
  site-data.ts      — ALL content constants: NAV_LINKS, MIXES, HEARTHIS_TRACKS,
                       VIDEOS, SOCIAL_LINKS, STATS, PHOTOS — edit content here
  site-url.ts       — validated BASE_URL (validates NEXT_PUBLIC_SITE_URL env var,
                       falls back to https://dj-kimchi.rauell.systems)
```

---

## Critical Rules

### Page must stay a Server Component
`src/app/page.tsx` must NOT have `"use client"` at the top. Individual section components handle their own interactivity. Removing SSR breaks Google/Bing indexing.

### Content lives in site-data.ts
All text, links, mixes, photos, and social URLs are constants in `src/lib/site-data.ts`. Never hardcode content in components.

### Multi-page routing — do NOT use hash anchors
The site has 5 real Next.js routes: `/`, `/about`, `/music`, `/videos`, `/bookings`.
Navigation uses `<Link href="...">` from `next/link`, NOT `<a href="#...">` hash anchors.
Each page file is a server component that exports `metadata` with a unique canonical URL.
`NAV_LINKS` in `site-data.ts` defines the route paths — update content there, not in components.

### Email
Booking form emails via Resend. Requires `RESEND_API_KEY` env var.

---

## SEO

- Sitemap: `/sitemap.xml` — generated by `src/app/sitemap.ts` (5 routes; BASE_URL from `src/lib/site-url.ts`)
- Robots: `/robots.txt` — generated by `src/app/robots.ts`
- JSON-LD: `Person` + `MusicGroup` + `WebSite` schema in `layout.tsx`
- Canonical + OG metadata in `layout.tsx`
- Google verification: meta tag `google-site-verification` in `layout.tsx`
- Bing verification: meta tag `msvalidate.01` in `layout.tsx`
- Verification files: `public/BingSiteAuth.xml`, `public/googlee69e3a7319b06c7f.html`

---

## Social Profiles (for sameAs in JSON-LD)

- Instagram: https://www.instagram.com/dj_kimchii/
- YouTube: https://www.youtube.com/@djkimchi254
- Facebook: https://www.facebook.com/djkimchii/
- Mixcloud: https://www.mixcloud.com/djkimchii254/
- HearThis: https://hearthis.at/djkimchii254-ja/

---

## Dev Commands

```bash
bun install
bun run dev          # http://localhost:3000
bun run build        # production build + static copy
bun run lint
bun run map:update   # refresh CODEBASE_DEBUG_MAP.md
```

---

## MCP Tools: code-review-graph

This project has a knowledge graph. Use `semantic_search_nodes` or `query_graph` BEFORE Grep/Glob/Read for code exploration. Fall back to file tools only when the graph doesn't cover what you need.

| Tool | Use when |
|------|----------|
| `detect_changes` | Code review — gives risk-scored analysis |
| `get_impact_radius` | Understand blast radius of a change |
| `query_graph` | Trace callers, callees, imports |
| `semantic_search_nodes` | Find functions/classes by keyword |
| `get_architecture_overview` | High-level structure |

---

## Workspace Map

Read `CODEBASE_DEBUG_MAP.md` first before deep debugging. Refresh with `bun run map:update` after structural changes.
